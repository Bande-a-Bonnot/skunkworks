# Experiment Agent Notes

Local instructions for this experiment. Replace this template with experiment-specific context.

## Start Here

1. Read this file.
2. Read `docs/HANDOFF.md`.
3. Read `README.md`.
4. Review local todos in `todos/`.
5. Check repo state from the repository root:
   ```bash
   git status --short --untracked-files=all
   ```

## Local Rules

- Keep all generated files and dependencies inside this experiment directory unless explicitly documented otherwise.
- Document setup and verification in this experiment's `README.md`.
- Update `docs/HANDOFF.md` before context compaction or meaningful session boundaries.
- If the experiment is abandoned or graduated, update both this README and `../README.md` catalog.
