---
status: pending
priority: p1
issue_id: "003"
tags: [algorithms, bfs, dijkstra]
dependencies: ["001", "002"]
---

# Implement BFS and Dijkstra

Implement the first traversal algorithms for the Core Data graph harness.

## Acceptance Criteria

- BFS returns expected traversal/reachability results on seeded graph fixtures.
- Dijkstra returns expected shortest path and distance on weighted fixtures.
- At least one comparison is documented between managed-object traversal and snapshot adjacency traversal.
- Findings are added to `README.md`, `docs/HANDOFF.md`, or `docs/solutions/`.
